<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="utf-8">
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <title>Tuan Diep's Wedding Invitation</title>
  <style>
    #unity-canvas {
      position: fixed;
      background: #ffffff;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      z-index: 0;
    }

    #progress-container {
      position: fixed;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      background: rgba(0, 0, 0, 0.5);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 10;
    }

    #progress-bar {
      width: 50%;
      background: #000;
      border: 5px solid #000;
      
      overflow: hidden;
    }

    #progress-bar-inner {
      height: 30px;
      width: 0;
      background: #fff;
    }
  </style>
</head>
<body style="text-align: center; padding: 0; border: 0; margin: 0;">
  <div id="progress-container">
    <div id="progress-bar">
      <div id="progress-bar-inner"></div>
    </div>
  </div>
  <canvas id="unity-canvas"></canvas>
  <script src="Build/Build_WebGL2.loader.js"></script>
  <script>
    function resizeCanvas() {
      var canvas = document.getElementById('unity-canvas');
      var windowWidth = window.innerWidth;
      var windowHeight = window.innerHeight;

      if (windowWidth / windowHeight < 9 / 16) {
        // If the window ratio is smaller than 9:16, adjust according to the width
        canvas.style.width = windowWidth + 'px';
        canvas.style.height = (windowWidth * 16 / 9) + 'px';
      } else {
        // If the window ratio is greater than or equal to 9:16, adjust according to the height
        canvas.style.width = (windowHeight * 9 / 16) + 'px';
        canvas.style.height = windowHeight + 'px';
      }
    }
    window.addEventListener('resize', resizeCanvas);
    window.addEventListener('load', resizeCanvas);

    createUnityInstance(document.querySelector("#unity-canvas"), {
      dataUrl: "Build/Build_WebGL2.data.unityweb",
      frameworkUrl: "Build/Build_WebGL2.framework.js.unityweb",
      codeUrl: "Build/Build_WebGL2.wasm.unityweb",
      streamingAssetsUrl: "StreamingAssets",
      companyName: "DefaultCompany",
      productName: "ChunChui",
      productVersion: "1.0",
    }, (progress) => {
      console.log(progress);
      var progressBarInner = document.getElementById('progress-bar-inner');
      progressBarInner.style.width = (progress * 100) + '%';
    }).then((unityInstance) => {
      resizeCanvas();
      var progressContainer = document.getElementById('progress-container');
      progressContainer.style.display = 'none';
    });
  </script>
</body>
</html>
